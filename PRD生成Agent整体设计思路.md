# PRD生成Agent整体设计思路

# 生成需求文档 Agent 整体设计思路

目标：输入一句话 / 原始想法、产品背景，Agent 自动输出结构化软件需求文档（PRD），包含业务背景、用户角色、功能需求、非功能需求、接口简述、验收标准、风险点。
核心痛点：单纯大模型直接写 PRD 容易：逻辑缺失、边界没考虑、需求模糊、缺少验收条件、遗漏非功能需求。所以 Agent 不能简单一个 prompt，要做**多步骤拆解 \+ 校验 \+ 结构化输出**。

整体架构：`用户输入 → 意图解析 → 业务信息补全 → 需求拆解Agent → 需求校验Agent → 文档组装 → 输出PRD`
可以基于 LangGraph / Dify 工作流实现。

## 1\. 输入层（用户输入）

用户输入形式多种多样：

- 简短想法：“做一个内部报销系统”

- 潦草草稿：一堆聊天记录、简单 markdown

- 原型截图描述、业务流程文本

输入字段定义：

```json
{
  "product_name":"",
  "business_background":"",
  "target_user":"",
  "raw_user_input":"用户原始想法",
  "constraint":["工期2个月","预算有限","必须对接企业微信"]
}
```

> 如果用户输入信息不全，Agent 可以选择反问补全信息，或者使用默认占位。
> 
> 

## 2\. 第一节点：信息解析 \& 补全 Agent

**职责：把模糊用户输入做结构化提取，识别缺失信息**
Prompt 核心任务：

1. 提取：产品名称、业务目标、使用用户、约束条件、核心业务价值

2. 识别缺失项：比如没有用户角色、没有业务目标

3. 两种分支：

    - 模式 A（交互模式）：信息不足，输出提问列表，反问用户补齐

    - 模式 B（自动生成模式）：不反问，基于行业常识补全假设，同时文档中标注【假设】

输出结构化 json，不要直接输出自然语言，给下游节点使用。

示例输出结构：

```json
{
  "product_name":"内部报销系统",
  "business_goal":"员工线上提交报销，财务审核，减少纸质单据",
  "user_roles":["普通员工","财务审核员","管理员"],
  "constraints":["对接企业微信","开发周期2个月"],
  "missing_info":["没有明确报销金额上限"],
  "is_info_enough":true
}
```

## 3\. 第二节点：业务拆解 Agent（核心）

输入上面结构化信息，**分模块生成需求素材，不要一次性生成完整文档**。
拆分成多个子任务，可以并行执行：

1. 业务背景与业务目标

2. 用户角色与用户故事 User Story

3. 主业务流程图（文本 Mermaid）

4. 功能需求：拆分为模块，每个功能点：编号、简述、输入、输出、操作步骤

5. 非功能需求：性能、安全、兼容性、合规、可用性

6. 接口简要清单（只做概要，不写详细字段）

7. 风险与依赖

> 关键点：用 User Story 格式 `作为[角色]，我希望[功能]，以便[价值]`，再扩展详细需求。
> 这里不要直接输出完整 PRD，输出一份中间结构化需求素材。
> 
> 

## 4\. 第三节点：需求校验评审 Agent（非常关键，区别普通 prompt）

模拟产品经理评审，对上一步产出的需求素材做检查，输出修改意见。
校验清单：

1. 是否有模糊需求（比如 “系统要快”，改成具体指标）

2. 是否缺少异常分支（报销被驳回、网络异常、权限越界）

3. 用户故事是否完整，每个需求是否可以可测试

4. 是否遗漏非功能需求：权限、数据安全

5. 是否冲突，需求之间互相矛盾

6. 有没有缺少验收标准

输出：`修改建议列表`，回传给上一步业务拆解 Agent 迭代优化需求素材。
可以设置迭代轮次，最多 2 轮，避免无限循环。

## 5\. 第四节点：文档组装 Agent

把校验完成后的结构化素材，渲染成标准 PRD 文档（Markdown 格式）。
标准 PRD 模板结构：

```Plain Text
# XX产品需求文档PRD
## 1 文档概述
1.1 文档目的
1.2 范围
1.3 修订记录

## 2 业务背景与目标
## 3 用户角色说明
## 4 业务流程图（mermaid）
## 5 功能需求
    5.1 模块1
        5.1.1 功能点
            用户故事、前置条件、操作流程、异常流程、验收标准
## 6 非功能需求
    性能、安全、权限、兼容性、可用性
## 7 外部依赖与接口概要
## 8 风险点与假设条件
## 9 附录
```

> 注意：把所有 Agent 的【假设】标记高亮，方便人阅读区分哪些是用户没说 AI 脑补的。
> 
> 

## 6\. 可选扩展能力

### 6\.1 输出产物多格式

- markdown PRD（主输出）

- 导出 json 结构化需求（方便下游：转测试用例 Agent、转开发任务拆解 Agent）

### 6\.2 工具调用增强

- 检索同类产品 PRD 范例作为参考

- mermaid 语法校验，检查流程图是否合法

### 6\.3 两种运行模式

1️⃣ **对话交互模式（推荐给人用）**
信息不全就反问用户，逐步收集信息，迭代生成 PRD。

2️⃣ **全自动模式（程序调用）**
不反问，全部自动假设，标记假设项，直接输出完整文档，适合嵌入其他系统。

## 7\. LangGraph 工作流简易流程图

```Plain Text
用户输入
    ↓
信息解析补全节点 → 判断信息是否充足
        ├─不充足 → 返回给用户提问
        └─充足 → 业务拆解节点
                ↓
            需求评审校验节点
                ↓
        判断是否需要迭代修改
            ├─是 → 回到业务拆解
            └─否 → PRD文档组装 → 输出markdown文档
```

## 8\. 关键 Prompt 设计技巧

1. **强制结构化输出**：要求输出 json，禁止自由文本，减少大模型幻觉乱输出。

2. **区分事实和假设**：所有 AI 推断出来的信息必须打上【假设】标签。

3. **强制每个功能点必须附带验收标准**，这是需求文档的灵魂，很多 AI 写 PRD 会漏掉。

4. 评审 Agent 要独立大模型调用，不要写在同一个 prompt 里面，分开调用效果更好。

## 9\. 可落地最小 demo 伪代码思路（Python）

```python
# 节点1：信息解析
info_struct = parse_user_input(raw_input)
if not info_struct["is_info_enough"]:
    return gen_question(info_struct)

# 节点2：拆解需求
req_material = decompose_business(info_struct)

# 节点3：评审校验
review_result = prd_reviewer(req_material)
max_loop = 2
for _ in range(max_loop):
    if review_result["need_fix"]:
        req_material = decompose_business(info_struct, review_result["suggestions"])
        review_result = prd_reviewer(req_material)
    else:
        break

# 节点4：组装文档
prd_markdown = render_prd(req_material)
print(prd_markdown)
```

## 10\. 后续可以延伸的 Agent 链路

PRD 生成完之后，可以串下游 Agent：
`PRD Agent → 测试用例生成Agent → 开发任务拆解Agent`

## 现存坑点要注意

1. 大模型容易脑补业务，**一定要标记假设项**，否则生成的 PRD 完全偏离用户真实想法。

2. 不要一次性让大模型写完整大文档，容易逻辑断裂，分步骤拆解质量高很多。

3. 一定要加异常流程，AI 经常只写正常 happy path。

> 
